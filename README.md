A word counter that count the word passed by a text area and show it.

**Run on Linux**
```code
pip3 install django
git clone https://github.com/srikant-panda/wordCounter.git
cd wordCounter.git
python3 -m venv .venv
source .venv/bin/activate
python3 manage.py
```
